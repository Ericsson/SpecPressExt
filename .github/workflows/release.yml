name: Build and Release VSIX

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - run: npm test

      - run: npx @vscode/vsce package --no-rewrite-relative-links --allow-missing-repository

      - name: Upload VSIX to Release
        uses: softprops/action-gh-release@v2
        with:
          files: '*.vsix'
